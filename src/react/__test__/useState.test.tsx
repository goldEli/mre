import React, { useState, render } from "../index"

// let flag = true
// const Counter = () => {
//   const [num, setNum] = useState(0)

//   flag && setTimeout(() => {
//     flag = false
//     setNum(num + 1)
//   }, 0)
//   return (
//     <div>{`counter: ${num}`}</div>
//   )
// }

describe("test useState", () => {
  it("The state should be changed by setState", () => {
    // jest.useFakeTimers();
    // const element = <Counter /> as any
    // let dom = element.getDom(0)
    // expect(dom.outerHTML).toBe("<div data-reactid=\"0\">counter: 0</div>")

    // render(<Counter />, document.body);


    // setTimeout(() => {
    //   dom = element.getDom(0)
    //   expect(dom.outerHTML).toBe("<div data-reactid=\"0\">counter: 1</div>")
    // }, 0);

    // jest.runAllTimers();
  })
})